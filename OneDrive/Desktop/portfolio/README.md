# Siri Shetty K — Data Science & AI/ML Portfolio

Premium multi-page portfolio built with React + Vite + Tailwind CSS + React Router.

## Setup

```bash
npm install
npm run dev
```

Build: `npm run build` · Preview: `npm run preview`

Vercel-ready. Deploy as a Vite app.
